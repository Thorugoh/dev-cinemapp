import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        marginBottom: 15
    },
    title: {
        fontSize: 40,
        color: '#FFF'
    },
    subtitle: {
        fontSize: 16,
        color: '#FFF'
    }
})

export default styles;
